# fluxo-codigo.md — #55 Gaveta de dinheiro (uso interno, NÃO publicar)

> ⛔ **DOCUMENTO INTERNO — NÃO PUBLICAR.** Aqui moram rota de API, nome de campo e
> nome de arquivo do código: é a anotação de quem **produziu** o manual, para quem
> for mexer nele depois. O lojista lê só o `gaveta-dinheiro.md` desta pasta.
> Se você é uma IA montando a página publicada, **pare de ler aqui**.

- Sem tela no `beefood-web-react`. `openDrawer` existe só na ponte Android (`ANDROID_PRINT.md`).
- Artigo antigo interno; print público do utilitário Control iD.

