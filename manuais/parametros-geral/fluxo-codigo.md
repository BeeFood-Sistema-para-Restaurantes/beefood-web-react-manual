# fluxo-codigo.md — Geral (#42)

> ⛔ **DOCUMENTO INTERNO — NÃO PUBLICAR.** Aqui moram rota de API, nome de campo e
> nome de arquivo do código: é a anotação de quem **produziu** o manual, para quem
> for mexer nele depois. O lojista lê só o `parametros-geral.md` desta pasta.
> Se você é uma IA montando a página publicada, **pare de ler aqui**.

| Tela | Flag | Uso |
|------|------|-----|
| Motivo de cancelamento | `motivoCancelamento` | cancelar item/venda **e** desconto (`useDescontoGuard`) |
| Operador | `operadorPDV` | `ModalValidarOperador` no PDV/Mesas com carrinho vazio e no Testar |
| (sem UI desktop) | `operadorPDVObrigar` | só API / mobile |

Vídeo embutido na rota (não repetir): Código do Operador `G5PH7VOlunw`.
