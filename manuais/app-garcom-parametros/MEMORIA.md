# MEMÓRIA — #40 App Garçom (parâmetros)

> ⛔ **DOCUMENTO INTERNO — NÃO PUBLICAR.** Aqui moram rota de API, nome de campo e
> nome de arquivo do código: é a anotação de quem **produziu** o manual, para quem
> for mexer nele depois. O lojista lê só o `app-garcom-parametros.md` desta pasta.
> Se você é uma IA montando a página publicada, **pare de ler aqui**.

Status: ✅ 21/08/2026. Só a tela de configuração — sem print de celular, conforme o plano.

Flags: `appGarcomComanda`, `appGarcomMesa`, `appGarcomNomeAvulso`, `appGarcomCliente`. Os quatro ficaram ON no sandbox.

Frase obrigatória no texto: *“Este parâmetro vale no aplicativo do garçom, que não faz parte deste manual.”*

Não misturar com taxa/obrigatoriedades (#41) nem com `caixaPorUsuario` (#13).
